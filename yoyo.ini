[DEFAULT]
sources = migrations
database = ${DEV_DATABASE_URL}
migration_table = _yoyo_migration
batch_mode = off
verbosity = 0

[dev]
database = ${DEV_DATABASE_URL}

[test]
database = ${TEST_DATABASE_URL}